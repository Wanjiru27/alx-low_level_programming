#include "main.h"

/**
 * _puts - print to the stdout
 * @str: string
 *
 * Return: void
 */
void _puts(char *str)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	_puts(str);
	return (0);
}
